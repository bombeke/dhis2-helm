{{- $fullName := include "dashboard.fullname" . -}}
{{- if and .Values.persistence.enabled .Values.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullName }}-drupal-pvc
  labels:
    {{- include "dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/resource-policy": keep
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
{{- end }}


---

{{- $fullName := include "dashboard.fullname" . -}}
{{- if and .Values.persistence.enabled .Values.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $fullName }}-drupal-config-pvc
  labels:
    {{- include "dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/resource-policy": keep
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  resources:
    requests:
      storage: 1Gi
{{- end }}