{{- if .Values.ingress.enabled }}
{{- $fullname := include "dashboard.fullname" . }}
{{- $port := .Values.service.port }}
{{- $tls := hasKey .Values.ingress "certIssuer" -}}
{{- $annotations := merge (deepCopy .Values.ingress.annotations) .Values.commonAnnotations }}
{{- with .Values.ingress.certIssuer }}
{{- $_ := set $annotations "cert-manager.io/cluster-issuer" . }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullname }}
  labels:
    {{- include "dashboard.componentLabels" (dict "ctx" . "component" "web") | nindent 4 }}
  annotations:
  {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
  {{- end }}
    traefik.ingress.kubernetes.io/router.middlewares: {{ .Release.Namespace }}-{{include "dashboard.fullname" . }}-middleware@kubernetescrd
  {{- if $tls }}
    cert-manager.io/cluster-issuer: {{ .Values.ingress.certIssuer | quote }}
    ingress.kubernetes.io/ssl-redirect: "true"
  {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "dashboard.fullname" $ }}
                port:
                  number: {{ $.Values.service.port }}
          {{- end }}
    {{- end }}
{{- end }}

---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "dashboard.fullname" . }}-middleware
  labels:
{{- include "dashboard.labels" . | nindent 4 }}
spec:
  headers:
{{- with .Values.middleware.headers }}
  {{- toYaml . | nindent 4 }}
{{- end }}
    accessControlAllowOriginList:
{{ toYaml .Values.allowedOrigins | nindent 6 }}